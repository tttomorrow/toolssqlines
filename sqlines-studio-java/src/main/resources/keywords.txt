 ABS, ACOS, ADD, ADDDATE, ADD CONSTRAINT, ALL, ALTER, ALTER COLUMN, ALTER TABLE, AND, ANY,
 AS, ASC, ASCII, ASIN, ATAN, ATN2, AUTOINCREMENT, AVG,

 BACKUP DATABASE, BEGIN, BETWEEN, BIGINT, BIN, BINARY, BY,

 CASCADE, CASE, CAST, CEIL, CEILING, CHAR, CHARACTER_LENGTH, CHARINDEX, CHAR_LENGTH, CHECK,
 COALESCE, CLOSE, COLUMN, COMMENT, COMMIT, CONCAT, CONCAT_WS, CONNECT, CONNECTION_ID, CONSTRAINT,
 CONV, COUNT, CREATE, CREATE DATABASE, CREATE INDEX, CREATE OR REPLACE VIEW, CREATE TABLE,
 CREATE PROCEDURE, CREATE UNIQUE INDEX, CREATE VIEW, CURDATE, CURRENT_DATE, CURRENT_TIME,
 CURRENT_TIMESTAMP, CURRENT_USER, CURSOR, CURTIME,

 DATABASE,  DATALENGTH, DATE, DATEADD, DATETIME, DATEFROMPARTS, DATENAME, DATEPART, DATE_ADD,
 DATE_FORMAT, DATE_SUB, DAY, DAYNAME, DAYOFMONTH, DAYOFWEEK, DAYOFYEAR, DEALLOCATE, DECLARE, DECODE,
 DEGREES, DELETE, DEFAULT, DELETE, DESC, DIFFERENCE, DISTINCT, DIV, DOUBLE, DROP, DROP COLUMN,
 DROP CONSTRAINT, DROP DATABASE, DROP DEFAULT, DROP INDEX, DROP TABLE, DROP VIEW,

 ELSE, END, EXCEPTION, EXEC, EXISTS, EXIT, EXP, EXTRACT,

 FETCH, FIELD, FIND_IN_SET, FLOOR, FOR, FOREACH, FOREIGN, FOREIGN KEY, FORMAT, FROM, FROM_DAYS,
 FULL OUTER JOIN, FUNCTION,

 GETUTCDATE, GREATEST, GROUP BY, GO,

 HAVING, HANDLER, HOUR,

 IF, IIF, IFNULL, IN, INDEX, INNER JOIN, INSERT, INSERT INTO, INSERT INTO SELECT, INSTR, IS, ISDATE,
 ISNUMERIC, IS NULL, IS NOT NULL, INT, INTO,

 JOIN,

 KEY,

 LAST_DAY, LAST_INSERT_ID, LEFT, LEFT JOIN, LEN, LENGTH, LEAST, LIKE, LIMIT, LN, LOCALTIME,
 LOCALTIMESTAMP, LOCATE, LOG, LOG2, LOG10, LONG, LOOP, LOWER, LPAD, LTRIM,

 MAKEDATE, MAKETIME, MAX, MICROSECOND, MID, MIN, MINUTE, MOD, MONEY, MONTH, MONTHNAME,

 NCHAR, NOT, NOW, NOT NULL, NULL, NULLIF, NUMBER, NUMERIC, NVARCHAR, NVARCHAR2,

 ON, OPEN, OR, ORDER BY, OUT, OUTER JOIN, OVER,

 PATINDEX, PI, POSITION, POW, POWER, PRIMARY, PRIMARY KEY, PROCEDURE,

 QUARTER, QUOTENAME,

 RADIANS, RAND, REFERENCES, REPEAT, REPLACE, REPLICATE, RESTRICT, RETURN, RETURNS, RETURNING,
 REVERSE, RIGHT, ROUND, ROWNUM, RPAD, RTRIM,

 SECOND, SEC_TO_TIME, SELECT, SELECT DISTINCT, SELECT INTO, SELECT TOP, SESSIONPROPERTY,
 SESSION_USER, SET, SHORT, SIGN, SIGNED, SIN, SMALLINT, SMALLMONEY, SOUNDEX, SPACE, STR, STRCMP,
 STUFF, STR_TO_DATE, SQUARE, SQRT, SUBDATE, SUM, SUBSTR, SUBSTRING, SUBSTRING_INDEX, SUBTIME,
 SYSDATE, SYSDATETIME, SYSTEM_USER,

 TAN, TABLE, THEN, TIME, TIMEDIFF, TIMESTAMP, TIME_FORMAT, TIME_TO_SEC, TOP, TO_DAYS, TRANSLATE,
 TRIM, TRUNCATE, TRUNCATE TABLE,

 UCASE, UNICODE, UNION, UNION ALL, UNIQUE, UNIQUEIDENTIFIER, UNSIGNED, UPDATE, UPPER, USER, USER_NAME,

 VALUES, VARCHAR, VARCHAR2, VERSION, VIEW, VOLATILE,

 WEEK, WEEKDAY, WEEKOFYEAR, WHEN, WHERE, WHILE, WORK,

 XML,

 YEAR, YEARWEEK
